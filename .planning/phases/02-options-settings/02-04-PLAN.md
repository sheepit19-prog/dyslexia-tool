---
phase: 02-options-settings
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User confirms options page loads and all 6 sections are navigable"
    - "User confirms settings auto-save and persist across reload"
    - "User confirms visual layout is spacious and readable"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete options page.

Purpose: Confirm the options page works end-to-end as a Chrome extension options page, not just in dev mode.
Output: Verified, production-ready options page.
</objective>

<execution_context>
@C:/Users/berks/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/berks/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-options-settings/02-CONTEXT.md
@.planning/phases/02-options-settings/02-01-SUMMARY.md
@.planning/phases/02-options-settings/02-02-SUMMARY.md
@.planning/phases/02-options-settings/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete options page end-to-end</name>
  <files></files>
  <action>
Human verification of the complete options/settings page.

What was built — 6 sidebar-navigated sections:
1. General Settings — font toggle, font family, line/letter spacing, theme, accent color
2. Companion Settings — mode select, sensitivity slider, analytics toggle
3. Notes Settings — TTS speed, monthly note count
4. Hotkeys — keyboard shortcuts reference table
5. Per-Site Preferences — card list with expand/edit/remove overrides
6. Privacy — export data, delete all data (with DELETE confirmation), storage usage

Steps to verify:
1. Run `cd apps/extension && npm run build`
2. Load extension in Chrome (chrome://extensions → Developer mode → Load unpacked → select dist folder)
3. Right-click extension icon → "Options" to open options page
4. Click each of the 6 sidebar categories — each should show its settings panel
5. Toggle "Dyslexia Font" on → check for "Saved" indicator → refresh page → confirm toggle is still on
6. Adjust line spacing to 2.0 → confirm value updates → refresh → persists
7. Select companion mode "Off" → confirm value persists
8. Check hotkeys table shows 4 commands with shortcuts or "Not set" badges
9. If no per-site overrides, confirm empty state message shows
10. Click "Export My Data" → confirm JSON file downloads with settings, notes metadata, sitePreferences, analytics
11. Confirm storage stats show item counts per category
12. Confirm generous spacing, clear labels, comfortable reading experience
  </action>
  <verify>Human confirms all 12 verification steps pass</verify>
  <done>User approves the options page as functional and visually appropriate for dyslexic users</done>
</task>

</tasks>

<verification>
Human confirms all sections work, auto-save persists, export downloads correctly, layout is readable.
</verification>

<success_criteria>
User approves the options page as functional and visually appropriate for dyslexic users.
</success_criteria>

<output>
After completion, create `.planning/phases/02-options-settings/02-04-SUMMARY.md`
</output>
